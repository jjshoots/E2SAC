program: main.py
method: bayes
metric:
  name: max_eval_perf
  goal: maximize
parameters:
  # use_entropy:
  #   values: [true, false]
  target_entropy:
    min: -4.0
    max: +4.0
  confidence_scale:
    min: 0.10
    max: 10.0
command:
  - python3
  - src/main.py
  - "--train"
  - "--wandb"
